ingress:
  enabled: true
  name: voting-app-ingress
  className: nginx
  host: demo.local
  paths:
    - path: /vote
      serviceName: vote
      servicePort: 80
    - path: /result
      serviceName: result
      servicePort: 80
